# Vercel 대시보드 > Storage/Marketplace 탭에서 Upstash Redis 통합을 프로젝트에
# 연결하면 아래 두 값은 자동으로 채워진다. 로컬 개발 시에는 `vercel env pull`로 받아온다.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# 커스텀 도메인을 쓸 때만 고정값으로 지정 (안 쓰면 요청 Host 헤더에서 자동 계산됨)
# RP_ID=your-domain.com
